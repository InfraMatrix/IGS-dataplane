autoinstall:
  version: 1
  identity:
    hostname: ubuntu-base
    username: ubuntu
    # Force a password since this is just a base image
    password: "$6$xyz$rfUu2dY2pWkfAd6T4uOXsawChHINKQgePNOxjD.BBz0pi9N4RiPBEn.L5t9.3bpNhK3T1eUAaLg5srAu5oKyI."  # password is "ubuntu"
  ssh:
    install-server: true
    allow-pw: false
  storage:
    layout:
      name: direct
  user-data:
    disable_root: true
  late-commands:
    - apt-get install -y cloud-init
    - shutdown -h now
  early-commands:
    # Ensure our config is mounted
    - mkdir -p /mnt/config
    - mount -t 9p -o trans=virtio,version=9p2000.L config /mnt/config
  interactive-sections: []   # This should force no interaction